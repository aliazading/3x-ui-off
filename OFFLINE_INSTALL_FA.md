# راهنمای نصب آفلاین پنل 3x-ui

این راهنما برای کاربرانی است که قصد دارند پنل را روی سروری نصب کنند که دسترسی به اینترنت بین‌الملل ندارد (مانند برخی سرورهای داخل ایران).

## پیش‌نیازها
شما به یک کامپیوتر متصل به اینترنت نیاز دارید تا فایل‌های لازم را دانلود کنید.

### ۱. دانلود فایل‌های مورد نیاز
فایل‌های زیر را دانلود کرده و در یک پوشه قرار دهید:

1.  **فایل اصلی پنل (نسخه amd64):**
    [دانلود x-ui-linux-amd64.tar.gz](https://github.com/MHSanaei/3x-ui/releases/latest/download/x-ui-linux-amd64.tar.gz)
2.  **اسکریپت مدیریت:**
    [دانلود x-ui.sh](https://raw.githubusercontent.com/MHSanaei/3x-ui/main/x-ui.sh)
3.  **فایل سرویس (برای اوبونتو/دبیان):**
    [دانلود x-ui.service.debian](https://raw.githubusercontent.com/MHSanaei/3x-ui/main/x-ui.service.debian)
4.  **اسکریپت نصب:**
    [دانلود install.sh](https://raw.githubusercontent.com/MHSanaei/3x-ui/main/install.sh)

### ۲. انتقال فایل‌ها به سرور
تمام فایل‌های دانلود شده را به سرور خود منتقل کنید. می‌توانید از دستور `scp` یا نرم‌افزارهایی مثل **WinSCP** یا **FileZilla** استفاده کنید.
فایل‌ها را در یک پوشه (مثلاً `/root/x-ui-offline`) قرار دهید.

### ۳. اجرای نصب
وارد ترمینال سرور شوید و به پوشه‌ای که فایل‌ها را در آن کپی کرده‌اید بروید:

```bash
cd /root/x-ui-offline
```

سپس به اسکریپت نصب مجوز اجرا بدهید و آن را اجرا کنید:

```bash
chmod +x install.sh
./install.sh
```

اسکریپت به صورت خودکار فایل‌های محلی را تشخیص داده و بدون نیاز به اینترنت، پنل را نصب می‌کند.

## نکات مهم
*   **SSL:** برای نصب SSL نیاز به اینترنت است. اگر سرور شما به اینترنت دسترسی ندارد، باید از پنل بدون SSL استفاده کنید یا گواهینامه‌های خود را به صورت دستی وارد کنید.
*   **وابستگی‌ها:** اسکریپت سعی می‌کند `curl` ، `tar` و `openssl` را نصب کند. اگر سرور شما به مخازن داخلی (مثل ir.archive.ubuntu.com) دسترسی داشته باشد، این کار به راحتی انجام می‌شود. در غیر این صورت، مطمئن شوید این ابزارها از قبل روی سرور نصب هستند.
